import 'package:flutter/material.dart';

// Placeholder share tab while AppFlowy dependencies are decoupled

class ShareTab extends StatelessWidget {
  const ShareTab({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    // Placeholder implementation
    return const Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Text('Share Tab (Coming soon)'),
          SizedBox(height: 16),
          Text('Sharing features will be available after decoupling is complete'),
        ],
      ),
    );
  }
}
