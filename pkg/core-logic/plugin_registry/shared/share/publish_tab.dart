import 'package:flutter/material.dart';

// Placeholder publish tab while AppFlowy dependencies are decoupled

class PublishTab extends StatelessWidget {
  const PublishTab({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    // Placeholder implementation
    return const Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Text('Publish Tab (Coming soon)'),
          SizedBox(height: 16),
          Text('Publishing features will be available after decoupling is complete'),
        ],
      ),
    );
  }
}