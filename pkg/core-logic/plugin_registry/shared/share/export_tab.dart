import 'package:flutter/material.dart';

// Placeholder export tab while AppFlowy dependencies are decoupled

class ExportTab extends StatelessWidget {
  const ExportTab({super.key});

  @override
  Widget build(BuildContext context) {
    // Placeholder implementation
    return const Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Text('Export Tab (Coming soon)'),
          SizedBox(height: 16),
          Text('Export formats will be available after decoupling is complete'),
        ],
      ),
    );
  }
}